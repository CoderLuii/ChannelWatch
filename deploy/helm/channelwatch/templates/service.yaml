apiVersion: v1
kind: Service
metadata:
  name: {{ include "channelwatch.fullname" . }}
  labels:
    {{- include "channelwatch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "channelwatch.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
